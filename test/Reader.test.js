const Reader = require("./../lib/utils/Reader");

describe("Pruebas unitarias para explorers_launchx", () => {
    test('1. Leer el archivo y regresar la información', () => {
        
        
        expect().toBe();
    });
})